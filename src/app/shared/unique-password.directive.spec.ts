import { UniquePasswordDirective } from './unique-password.directive';

describe('UniquePasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new UniquePasswordDirective();
    expect(directive).toBeTruthy();
  });
});
