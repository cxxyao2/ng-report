import {
  UniqueUserDirective,
  UniqueUserValidator,
} from './unique-user.directive';

describe('UniqueUserDirective', () => {
  it('should create an instance', () => {
    // TODO
    // const directive = new UniqueUserDirective(new UniqueUserValidator());
    // expect(directive).toBeTruthy();
  });
});
